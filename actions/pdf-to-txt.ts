"use server"
import * as pdfjs from 'pdfjs-dist';

export const pdfToTxt = async (arrayBuffer: ArrayBuffer) => {
    try{

    }
    catch(error){
        console.log(error)
        return null
    }
}